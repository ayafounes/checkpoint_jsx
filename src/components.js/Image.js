// creation of the 4th component (Price) and exporting it to App.js

import React from 'react';
import product from './product';

function Image() {
  return  <img src={product.image}/>;
}

export default Image;
